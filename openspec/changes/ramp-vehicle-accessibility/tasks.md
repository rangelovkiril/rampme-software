## 1. Reference data (backend)

- [x] 1.1 Move `sofia_transport_low_floor.json` into `backend/src/gtfs/model-accessibility.json` (not `backend/data/` — that directory is gitignored runtime state, wrong home for a curated source file) and correct the only real mismatch against trinmo's own `specifications.accessibility` field (Waggon AG Be 4-6 → `false`; Т8М-700/M and Т8М-700IT already matched once "partial/mixed → not-equipped" is applied, no change needed there — see design.md Decisions); verified by re-diffing all 14 rows with a `specifications.accessibility` value against the file: zero disagreements.
- [x] 1.2 Cleaned up `model-accessibility.json`: dropped the per-row `source` field (citation clutter, not data the code reads), removed 6 entries that were never real passenger models (4 literal "unknown model" buckets, plus 2 rail-maintenance machines mislabeled under trinmo's TRAM type), fixed 2 keys silently dead since day one (plain space vs. trinmo's actual non-breaking space), and researched 8 of the original 16 null models to a real `true`/`false`. 2 models stayed `null` — genuinely inconclusive after research. Verified: 0 keys unmatched against trinmo's live model-name strings.
- [x] 1.3 Checked in a snapshot of the crawl's output as `backend/src/data/vehicle-accessibility.seed.json`, baked into the Docker image (`Dockerfile` `COPY`) as the fallback default for anyone running the image outside the fleet topology described in section 4 below — not itself the ongoing refresh mechanism. (Later moved from `gtfs/` to `data/` alongside `model-accessibility.json`, once nothing in `src/` imported either as code anymore — see 4.2.)

## 2. Backend enrichment

- [x] 2.1 Add `RAMP_ACCESSIBILITY_DATA_PATH` (default `./data/vehicle-accessibility.json`) and `RAMP_ACCESSIBILITY_REFRESH_MS` (default `3600000`) to `backend/src/config/index.ts`.
- [x] 2.2 Added `backend/src/gtfs/accessibility.ts`: `createAccessibilityResolver(dataPath, refreshMs)` (DI-factory, mirrors `createRampDb`/`createRampBridge`) loads the file at startup, reloads on the interval, and exposes `resolve(vehicleId): boolean | null` (strip `A`/`TM`/`TB` prefix → inventory → type-scoped lookup); `initAccessibility()`/`getAccessibility()` wire the production singleton, called from `index.ts`.
- [x] 2.3 Wired `resolveAccessibility` into `gtfs/enrich.ts` (new 4th param, replacing `trip?.wheelchair_accessible === 1`) and `routes/realtime.ts` (passes `getAccessibility().resolve`). **Scope note surfaced during implementation**: satisfying the spec's "Three distinguishable accessibility states" requirement needed a real code change beyond "thread hasRamp through" — `RampStatus` only had `unknown`/`working`/`in_use`, so a confirmed-not-equipped vehicle had nowhere to go but `unknown`. Added a 4th literal, `no_ramp`, updated `EnrichedVehicleSchema`'s union and the `has_ramp=true` filter. Added `test/services/ramp/status.test.ts` and `test/gtfs/enrich.test.ts`; `bun run test` and `bun run check` both pass.
- [x] 2.4 Updated `backend/AGENTS.md`'s environment variable table and structure tree.

## 3. Frontend visualization

- [x] 3.1 Extended `VehiclesLayer.tsx`'s marker rendering with an `accessibilityRingColor()`-driven border (green = ramp-equipped, gray = not-equipped, transparent = unknown) on both the detailed and dot icons, plus a matching text line in the popup; `lib/types.ts`'s `ramp_status` gained the `no_ramp` literal.
- [x] 3.2 Widened `mockTransitApi()` in `frontend/e2e/fixtures/transit.ts` with an optional `vehicles` array (defaults to today's single-vehicle behavior) and added a Playwright test asserting 3 vehicles in the 3 states render 3 distinct marker colors; `bun run test:e2e` — all 5 tests pass.

## 4. Delivery (gist + fleet — no code in either repo's CI)

- [x] 4.1 Published the crawl+join script as a public gist (`https://gist.github.com/rangelovkiril/8288bc4d272915fe86414526242fb276`, `refresh-accessibility.mjs`) — plain Node, zero dependencies, fetches `model-accessibility.json` from this repo's `raw.githubusercontent.com` URL for the join. Verified standalone (`node refresh-accessibility.mjs out.json`) and via the exact pipe the fleet workflow uses (`curl ... | node --input-type=module - out.json`); output matches the retired TypeScript version's, all previously-validated vehicle IDs resolve identically.
- [x] 4.2 Removed `backend/scripts/` (`refresh-accessibility.ts`, `trinmo-schemas.ts`) and `backend/src/gtfs/accessibility-table.ts` + its test from this repo — dead code once the crawl moved to the gist; nothing else referenced them. `bun run check`/`test` clean.
- [x] 4.3 Added `fleet/.github/workflows/refresh-accessibility.yaml`: weekly schedule (Monday 03:00 UTC) + `workflow_dispatch`, curls the gist and runs it, then writes a `ConfigMap` manifest directly with `echo`/`sed` (plain YAML wrapping a `|` block scalar — a single JSON blob under one key needs no templating tool, let alone a `kubectl` action) and commits via `stefanzweifel/git-auto-commit-action` using the workflow's default `GITHUB_TOKEN` — no PAT, no cross-repo checkout.
- [x] 4.4 Added `fleet/apps/rampme/backend/accessibility-refresh/` (`configmap.yaml` + `kustomization.yaml`, checked in with the current dataset) as a third sibling next to `base/` and `overlays/{production,stage}/` — not nested under either, since both overlays already merge `base/` into one `apps/` Kustomization, so a resource under `base/` or duplicated per-overlay would register twice and fail the build. Registered once in `apps/rampme/kustomization.yaml`.
- [x] 4.5 Moved the `ConfigMap` volume/mount/env var into `fleet/apps/rampme/backend/base/deployment.yaml`, so production and stage share exactly one `ConfigMap` (fixed one landmine: `overlays/stage/deployment-patch.yaml` is a JSON6902 patch whose `env` op *replaces* the whole array rather than merging, so `RAMP_ACCESSIBILITY_DATA_PATH` had to be added there explicitly too). Verified `kubectl kustomize` on the full `apps/` tree renders every resource exactly once, both Deployments reference the same `ConfigMap` name.
- [x] 4.6 Added a fleet wiki runbook entry (EN + BG `Operations` pages) documenting the `ConfigMap`/gist/workflow delivery mechanism, why it isn't a `PersistentVolume`, and how to trigger the first run manually.

## 5. Demo readiness

- [x] 5.1 Ran the crawl against production trinmo data and verified end-to-end locally: started the backend against live GTFS-RT with the freshly built dataset, `GET /realtime/vehicles` returned live vehicles with `working`, `no_ramp`, and `unknown` all genuinely present, not just theoretical.
- [x] 5.2 Ran a daytime validation pass (200 concurrent vehicles: 116 bus, 44 tram, 40 trolleybus, vs. the original 8-vehicle night sample). Match rate: **79.5% overall** (bus 87.1%, tram 77.3%, trolleybus 60.0%). Also confirmed the trolleybus `TB` prefix assumption live.

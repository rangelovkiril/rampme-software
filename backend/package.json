{
  "name": "backend",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "check": "biome check . && bunx tsc --noEmit",
    "fix": "biome check --fix ."
  },
  "dependencies": {
    "elysia": "latest",
    "mqtt": "^5.15.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
